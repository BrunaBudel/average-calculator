import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  header: {
    paddingHorizontal: 24,
    paddingBottom: 16,
    backgroundColor: '#fff',
  },
  title: {
    color: '#000',
    fontWeight: 'bold',
    fontSize: 20,
  },
  subtitle: {
    color: '#656F77',
    fontSize: 16,
  },
})
